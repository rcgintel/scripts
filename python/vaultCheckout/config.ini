[MTL128A0]
vaultCommandBasic = vault query -column location version milestone block  -n 1 -milestone A0_LV -domain sd
vaultCommandMilestone = vault query -column location version milestone block  -n 1 -milestone

[MTL128B0]
vaultCommandBasic = vault query -column location version milestone block  -n 1 -milestone B0_TR -domain sd
vaultCommandMilestone = vault query -column location version milestone block  -n 1 -milestone

[LNLA0]
vaultCommandBasic = vault query -column location version milestone block  -n 1 -milestone A0_LV -domain sd
vaultCommandMilestone = vault query -column location version milestone block  -n 1 -milestone

[WCLA0]
vaultCommandBasic = vault query -column location version milestone block  -n 1 -milestone sd0p8 -domain sd
vaultCommandMilestone = vault query -column location version milestone block  -n 1 -milestone





